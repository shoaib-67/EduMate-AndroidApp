name: edumate
description: "EduMate - University admission preparation platform"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.4.0
  provider: ^6.1.0
  flutter_secure_storage: ^9.2.0
  google_fonts: ^6.2.0
  fl_chart: ^0.70.0
  cached_network_image: ^3.4.0
  shimmer: ^3.0.0
  intl: ^0.20.0
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
